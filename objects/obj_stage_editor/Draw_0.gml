draw_resizable_ext(bgSprite,x, y, width, height, 0, 0);

var stageBoxX = x + width + 10; 
var stageBoxY = y; 

var stageBoxWidth = 100;
var stageBoxHeight = height;

draw_resizable_ext(spr_frame, stageBoxX, stageBoxY, stageBoxWidth, stageBoxHeight, 0, 0);

