x += .5 * node_size
y += .5 * node_size
pImg = choose(0,1,2)

depth = -2

direction =irandom_range(-10, 10);

speed = choose(1, );

image_speed = 0.5;

angle = irandom(360);

spin = choose(-1, 1);

scale = choose(2, 3);

alpha = 1;

color = c_white;
